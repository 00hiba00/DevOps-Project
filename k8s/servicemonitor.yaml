apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bookstore-servicemonitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: bookstore
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
